# EVVM Boilerplate

This is a Next.js project that serves as a boilerplate for applications using EVVM.

## Quickstart

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/evvm/boilerplate.git
    ```
2.  **Install dependencies:**
    ```bash
    cd boilerplate
    npm install
    ```
3.  **Set up your environment variables:**
    Rename `.env.example` to `.env` and add your project-specific variables.
4.  **Run the development server:**
    ```bash
    npm run dev
    ```
    Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Resources

- [EVVM Documentation](https://evvm.info)
- [Next.js Documentation](https://nextjs.org/docs)

